import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gayatri.practiceIonic',
  appName: 'practiceionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
